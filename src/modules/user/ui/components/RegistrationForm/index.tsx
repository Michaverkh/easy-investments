import { Box, Card, Tab, Tabs, Typography } from "@mui/material";
import { FC, SyntheticEvent, useState } from "react";

export const RegistrationForm: FC = () => {
  return (
    <>
      <Typography variant="h1">зарегистрируйтесь</Typography>
    </>
  );
};
